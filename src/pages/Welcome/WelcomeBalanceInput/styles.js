import { StyleSheet } from 'react-native';
import Colors from '../../../styles/Colors';

export default StyleSheet.create({
    label: {
        color: Colors.white,
        fontSize: 28,
        textAlign: 'center',
    },
});